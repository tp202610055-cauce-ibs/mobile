{
  "name": "cauce-openapi-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Toolchain de generacion del cliente Dart tipado desde el snapshot OpenAPI del backend Cauce (actas M16 y M20).",
  "scripts": {
    "generate": "openapi-generator-cli generate -c generator-config.yaml",
    "postgenerate": "cd ../../packages/cauce_api_client && dart pub get && dart run build_runner build --delete-conflicting-outputs"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "2.13.4"
  }
}
